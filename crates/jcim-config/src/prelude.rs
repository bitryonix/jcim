//! Convenience re-exports for the most common JCIM configuration types.

pub use crate::config::{BackendConfig, RuntimeConfig};
pub use crate::project::{
    ArtifactKind, BuildKind, ManagedPaths, ProjectAppletConfig, ProjectBuildConfig,
    ProjectCardConfig, ProjectConfig, ProjectMetadataConfig, ProjectSimulatorConfig,
    ProjectSourceConfig, UserConfig,
};
