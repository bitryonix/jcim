# jcim-app

`jcim-app` is the transport-neutral application core for JCIM 0.2.

It owns:

- project creation, discovery, and registry
- build-on-demand orchestration
- simulator lifecycle
- physical-card service flows behind an injectable adapter boundary
- machine-local setup and doctor behavior
